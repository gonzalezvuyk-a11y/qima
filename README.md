
# QIMA

Landing page built with Vite and React.

The source was migrated from the provided `Follow Instructions.zip` bundle into this repository so it can be versioned and deployed from `qima`.

## Development

Install dependencies:

```bash
npm install
```

Start the local dev server:

```bash
npm run dev
```

Create a production build:

```bash
npm run build
```
  
